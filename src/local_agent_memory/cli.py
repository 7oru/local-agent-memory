"""Command-line entrypoint placeholder.

The full command surface is implemented in the CLI milestone.
"""


def main(argv: list[str] | None = None) -> int:
    _ = argv
    print("local-agent-memory scaffold is installed; CLI commands arrive in M3.")
    return 0
